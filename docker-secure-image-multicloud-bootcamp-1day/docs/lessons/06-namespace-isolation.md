# Lesson 06 — Namespaces & cgroups (Crash Course)

Docker isolates processes with namespaces: PID, NET, MNT, UTS, IPC, USER.
- Combine with **cgroups** for resource limits.
- Harden with user namespaces and remap root where possible.
